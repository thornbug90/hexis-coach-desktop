mutation createRecurringWorkout($input: CreateWorkoutInput!) {
  createRecurringWorkout(input: $input) {
    id
    activity {
      id
    }
    intensity
    slot
    start
    end
    key
    competition
    recurring
  }
}
