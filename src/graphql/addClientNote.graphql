mutation addClientNote($input: NoteInput!) {
  addClientNote(input: $input) {
    id
    coachId
    clientId
    title
    body
    createdAt
    dayNoteDay
    utcOffset
    type
    alertNotification
  }
}
