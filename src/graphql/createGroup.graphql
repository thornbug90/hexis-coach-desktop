mutation createAGroup($input: CreateGroupInput!) {
  createGroup(input: $input) {
    id
    name
    link
    description
    createdAt
    coach {
      id
      firstName
      lastName
    }
  }
}
